const loading = () => {
  const html = `
  <div class="animate-ping absolute inline-flex h-full w-full rounded-full bg-purple-400 opacity-75 delay-1000" >
  
  </div>
  `;

  return html;
};

export default loading;